import fs from 'node:fs';
import path from 'node:path';
import { execFileSync } from 'node:child_process';

const root = process.cwd();
const reportPath = path.join(root, 'reports/lyria-missing-stem-provider-real-demo-latest.json');
const report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
const sourcePath = path.join(root, 'public', report.output.audioUrl.replace(/^\//, ''));
if (!fs.existsSync(sourcePath)) throw new Error(`Missing provider output: ${sourcePath}`);
const outputDir = path.join(root, 'public/review/lyria-atomic-material-gate-v1');
const audioDir = path.join(outputDir, 'audio');
fs.mkdirSync(audioDir, { recursive: true });
const fileName = 'support-bed-001.mp3';
const destination = path.join(audioDir, fileName);
if (!fs.existsSync(destination)) {
  try { fs.linkSync(sourcePath, destination); } catch { fs.copyFileSync(sourcePath, destination); }
}
const probe = JSON.parse(execFileSync('ffprobe', ['-v', 'error', '-show_entries', 'format=duration:stream=codec_name,sample_rate,channels', '-of', 'json', sourcePath, { encoding: 'utf8' }]));
const stream = probe.streams?.[0] ?? {};
const duration = Number(probe.format?.duration ?? 0);
const escapeHtml = (value) => String(value).replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[character]));
const html = `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Lyria 原子素材 Gate</title><style>
:root{color-scheme:dark;--bg:#111311;--panel:#1a1f1b;--line:#3b473d;--text:#f0f3ef;--muted:#aab5ac;--accent:#e0c47b}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:850px;margin:auto;padding:26px 18px 72px}.eyebrow{color:var(--accent);font-size:12px;font-weight:700}h1{font-size:28px;margin:8px 0 12px}.intro{border-bottom:1px solid var(--line);padding-bottom:22px}.intro p,.note{color:var(--muted);line-height:1.55}.status{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--line);margin:20px 0}.status div{background:var(--panel);padding:14px}.status strong{display:block;font-size:16px}.status span{color:var(--muted);font-size:12px}.pass{color:#a5d1a8}.pending{color:var(--accent)}.track{padding:24px 0;border-bottom:1px solid var(--line)}h2{font-size:20px;margin:0 0 5px}.meta{color:var(--muted);font-size:13px;margin:0 0 16px}audio{width:100%;height:42px}.prompt{margin-top:18px;background:var(--panel);border:1px solid var(--line);padding:14px;border-radius:6px;color:var(--muted);font-size:13px;line-height:1.5}.review-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:18px}label{display:grid;gap:5px;color:var(--muted);font-size:12px}select,textarea{width:100%;border:1px solid var(--line);border-radius:5px;background:#151915;color:var(--text);padding:9px;font:inherit}.notes{margin-top:10px}.actions{display:flex;gap:10px;margin-top:18px}button{border:1px solid var(--line);border-radius:6px;background:var(--accent);color:#171811;padding:10px 14px;font-weight:700;cursor:pointer}@media(max-width:560px){main{padding:18px 12px 55px}.status,.review-grid{grid-template-columns:1fr}}
</style></head><body><main><section class="intro"><p class="eyebrow">GOOGLE VERTEX AI LYRIA · PROVIDER GATE</p><h1>原子素材能力验证</h1><p>这不是成品曲目试听，而是验证商业 API 能否按要求产出一个真正可拆分、可单独使用的基础 Stem。本次请求只针对一个缺失的低动态支持层，结果不会自动进入生产库。</p><div class="status"><div><strong class="pass">API 返回成功</strong><span>真实 Vertex 请求</span></div><div><strong class="pass">文件可解码</strong><span>${duration.toFixed(2)} 秒 · ${escapeHtml(stream.codec_name ?? 'unknown')}</span></div><div><strong class="pending">元素资格待判定</strong><span>必须人工试听</span></div></div></section><section class="track"><h2>support-bed-001 · music.support_bed</h2><p class="meta">${(Number(stream.sample_rate) || 0).toLocaleString()} Hz · ${stream.channels ?? '-'} 声道 · ${Math.round(fs.statSync(sourcePath).size / 1024)} KB</p><audio controls preload="metadata" src="./audio/${fileName}"></audio><div class="prompt"><strong>实际请求：</strong>${escapeHtml(report.generationDecision.prompt)}</div><div class="review-grid"><label>是否是单一可拆分层<select data-field="isolated"><option value="">未判断</option><option value="yes">是</option><option value="no">否</option><option value="uncertain">不确定</option></select></label><label>是否有人声<select data-field="voice"><option value="">未判断</option><option value="none">无人声</option><option value="present">有人声</option><option value="uncertain">不确定</option></select></label><label>是否有主旋律<select data-field="melody"><option value="">未判断</option><option value="none">无主旋律</option><option value="present">有主旋律</option><option value="uncertain">不确定</option></select></label><label>是否有鼓点/脉冲<select data-field="beat"><option value="">未判断</option><option value="none">没有</option><option value="present">有</option><option value="uncertain">不确定</option></select></label><label>是否适合作为 support bed<select data-field="usable"><option value="">未判断</option><option value="yes">可以</option><option value="no">不可以</option><option value="conditional">需裁剪/处理</option></select></label><label>最终 Gate<select data-field="decision"><option value="">待审核</option><option value="pass">通过此类素材测试</option><option value="fail">未通过</option><option value="retry">改提示词重测</option></select></label></div><label class="notes">听感与问题<textarea data-field="notes" rows="4" placeholder="是否听到完整曲子感、明显旋律、鼓点、音色跳变或不能循环的结构"></textarea></label></section><div class="actions"><button id="export" type="button">导出 Gate 结果</button></div><p class="note">当前规则：只有“单一可拆分层=是、无人声、无主旋律、无鼓点、可作为 support bed、最终 Gate=通过”才允许进入下一批原子素材生产。否则只记录为 Provider 能力失败样本。</p></main><script>const key='snooze-lyria-atomic-material-gate-v1';const state=JSON.parse(localStorage.getItem(key)||'{}');document.querySelectorAll('[data-field]').forEach(input=>{input.value=state[input.dataset.field]||'';const save=()=>{state[input.dataset.field]=input.value;localStorage.setItem(key,JSON.stringify(state))};input.addEventListener('change',save);input.addEventListener('input',save)});document.getElementById('export').addEventListener('click',()=>{const output={schemaVersion:'1.0.0',reviewedOn:new Date().toISOString(),provider:'google-cloud-vertex-ai',model:${JSON.stringify(report.model)},sourceReport:'reports/lyria-missing-stem-provider-real-demo-latest.json',requestType:'single_missing_stem',review:state};const url=URL.createObjectURL(new Blob([JSON.stringify(output,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='lyria-atomic-material-gate-result-v1.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),0)});</script></body></html>`;
fs.writeFileSync(path.join(outputDir, 'index.html'), html);
console.log(`PASS: built ${path.join(outputDir, 'index.html')} for a real ${duration.toFixed(2)}s Lyria output`);
